entities:
{
	dynamicaddress:
	{
		namespace: "OUTRAGEdns.DynamicAddress"
		
		type: "dynamicaddress"
		route: "dynamicaddresses"
		table: "dynamic_addresses"
		
		actions:
		{
			add:    { id: false }
			edit:   { id: true }
			remove: { id: true }
			grid:   { id: false, default: true }
		}
	}
}