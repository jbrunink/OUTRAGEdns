entities:
{
	zone:
	{
		namespace: "OUTRAGEdns.Zone"
		
		type: "zone"
		table: "zones"
		
		actions:
		{
			add:    { id: false }
			edit:   { id: true }
			remove: { id: true }
			grid:   { id: false, default: true }
		}
	}
}