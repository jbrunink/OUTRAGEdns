entities:
{
	user:
	{
		namespace: "OUTRAGEdns.User"
		
		type: "user"
		table: "users"
		
		actions:
		{
			add:    { id: false }
			edit:   { id: true }
			remove: { id: true }
			grid:   { id: false, default: true }
			
			account: { id: false, global: true }
		}
	}
}