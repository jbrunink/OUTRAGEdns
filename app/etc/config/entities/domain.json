entities:
{
	domain:
	{
		namespace: "OUTRAGEdns.Domain"
		
		type: "domain"
		table: "domains"
		
		actions:
		{
			add:    { id: false }
			edit:   { id: true }
			remove: { id: true }
			export: { id: true }
			grid:   { id: false, default: true }
		}
	}
}